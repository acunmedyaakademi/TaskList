@using TaskList.Models.ViewModels;
@model List<JoinedTask>
<div class="col-md-11" style="margin:auto; margin-top:5vh;">
    <div class="card">
        <div class="card-header">
            <h3 class="card-title">Atanan Görevler       @ViewBag.DeleteTask</h3>
        </div>


        <div class="table-responsive">
            <table class="table card-table table-vcenter text-nowrap datatable">
                <thead>
                    <tr>

                        <th>Görev Atanan</th>
                        <th>Görev</th>
                        <th></th>
                        <th>Tarih</th>
                        <th>Durum</th>
                        <th></th>
                        <th>Yorumlar</th>
                        <th></th>
                        <th></th>

                    </tr>
                </thead>
                <tbody>
                    @if(Model!=null)
                    @foreach (var item in Model)
                    {
                        <tr>

                            <td><a class="text-reset" tabindex="-1">@item.AssignedByName</a></td>
                            <td>
                                @item.TaskName
                            </td>
                            <td>
                            </td>
                            <td>
                                @item.CreatedOn
                            </td>

                            @if (item.IsDone)
                            {
                                <td>
                                    <span class="badge bg-success me-1"></span> Tamamlandı
                                </td>
                            }

                            else
                            {
                                <td>
                                    <span class="badge bg-danger me-1"></span> Tamamlanmadı
                                </td>
                            }

                            <td></td>
                            <td class="text-nowrap">
                                <a href="#" class="text-muted">
                                    <!-- Download SVG icon from http://tabler-icons.io/i/message -->
                                    <svg xmlns="http://www.w3.org/2000/svg" class="icon" width="24" height="24" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" fill="none" stroke-linecap="round" stroke-linejoin="round"><path stroke="none" d="M0 0h24v24H0z" fill="none" /><path d="M4 21v-13a3 3 0 0 1 3 -3h10a3 3 0 0 1 3 3v6a3 3 0 0 1 -3 3h-9l-4 4" /><path d="M8 9l8 0" /><path d="M8 13l6 0" /></svg>
                                 @*yorumlar eklenecek*@
                                </a>
                            </td>
                            <td>
                                <a href="/task/updatetask?id=@item.Id" class="btn btn-warning">Düzenle</a>
                            </td>
                            <td>
                                <a href="/task/deletetask?id=@item.Id" class="btn btn-danger">Sil</a>
                            </td>
                        </tr>
                    }


                </tbody>
            </table>
        </div>

    </div>
</div>